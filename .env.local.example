# Convex (auto-populated by `npx convex dev`)
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=

# ── Convex Dashboard Environment Variables ──
# These are NOT set here. Set them at:
# https://dashboard.convex.dev → Settings → Environment Variables
#
# Required for auth (generated by `npx @convex-dev/auth`):
#   SITE_URL=http://localhost:3000
#   JWT_PRIVATE_KEY=...
#   JWKS=...
#
# Required for OAuth (optional — email/password works without these):
#   AUTH_GITHUB_ID=
#   AUTH_GITHUB_SECRET=
#   AUTH_GOOGLE_ID=
#   AUTH_GOOGLE_SECRET=
